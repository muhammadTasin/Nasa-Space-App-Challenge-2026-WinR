CLAUDE.md

# secrets - copy .env.example to .env locally, never commit it
.env
.netrc

# research data cache (regenerate with research/acquire/*.py; ~40 MB and growing)
research/data/

# source PDFs the team collected by hand: large and republished elsewhere; commit the extracted tables instead
raw, collected datas/*.pdf

__pycache__/
*.pyc
.venv/
